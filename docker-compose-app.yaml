services:
  app:
    image: icaroquintella/go_ci:latest
    Ports:
    - 8080:8080
    environment:
      - HOST=postgres 
      - DBPORT=5432
      - USER=root 
      - PASSWORD=root 
      - DBNAME=root